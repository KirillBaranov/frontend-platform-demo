{
  "name": "@platform/module-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo 'Template module — copy and rename to create a new module'",
    "build": "echo 'Template module — no build needed'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "echo 'No tests'",
    "clean": "echo 'nothing to clean'"
  },
  "dependencies": {
    "@platform/contracts": "workspace:*",
    "@platform/data-source": "workspace:*",
    "@platform/ui-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
