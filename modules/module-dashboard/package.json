{
  "name": "@platform/module-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "vue-tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "analyze": "vite build --mode analyze",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@platform/contracts": "workspace:*",
    "@platform/data-source": "workspace:*",
    "@platform/ui-kit": "workspace:*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "typescript": "^5.6.0",
    "vue-tsc": "^2.2.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
